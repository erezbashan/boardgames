const admin = require('firebase-admin');
const serviceAccount = require('./deployments/backend/serviceAccountKey.json'); // assuming this exists or I can just use default if running locally? Wait, local emulator?
